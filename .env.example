# URL de la config mviewer chargée automatiquement au démarrage (optionnel).
# Si absent, le serveur démarre sans contexte — l'agent devra appeler load_xml() manuellement.
# DEFAULT_CONFIG_URL=https://geobretagne.fr/apps/viz/config.xml
